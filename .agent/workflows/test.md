---
description: Run unit and E2E tests with auto-approval
---

// turbo-all

Follow these steps to verify the application:

// turbo
1. Run unit tests using Vitest: `npm test`

// turbo
2. Run E2E tests using Playwright: `npm run test:e2e`
