Languages: English, French, and Arabic.

RTL Support: Use Tailwind CSS logical properties (e.g., start and end instead of left and right) to ensure the layout mirrors correctly for Arabic.

Bi-directionality: Use <html dir='rtl'> when the active language is Arabic.
